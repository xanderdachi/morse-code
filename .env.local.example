# Copy to .env.local and fill in once Supabase is wired up.
# Vite only exposes variables prefixed with VITE_ to the client.
# On Cloudflare Pages, set these under Settings → Environment variables instead.

# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
